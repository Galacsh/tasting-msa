spring.cloud.gateway:
  routes:
    # signup, login, logout, password-reset, jwt refresh
    - id: auth
      uri: lb://auth
      predicates:
        - Path=/auth/**
      filters:
        - AuthFilter
        - RewritePath=/auth/?(?<path>.*), /$\{path}